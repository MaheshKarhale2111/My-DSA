#include <bits/stdc++.h>
using namespace std;

class Node {
    public: 
    int data; 
    Node* prev; 
    Node* next; 

    // constructor 
    Node(int n){
        this->data = n; 
        this->next = NULL;
    }
    
};

// push into stack 






int main(){

    

    // push(sp,1);
    // push(sp,12);
    // push(sp,2);
    // push(sp,6);
    // push(sp,51);
    // push(sp,17);

    // print(sp);

    
    return 0;
}